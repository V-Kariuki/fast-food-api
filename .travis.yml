
language: python
python:
  
  
  -"3.3"
  
# Command to install dependancies
install:
  -pip install -r requirements.txt
  

# command to run tests
script:
  -pytest 

after_success:
  -coveralls

after_script:
  -coveralls 



